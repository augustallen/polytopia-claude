Read-only review completed; no files changed. References to `plan.md` mean the supplied plan.

1. **P0 — Correct the victory condition.** `plan.md:81`, `107`, and `181` assume taking the capital finishes Domination. Single-player Domination requires eliminating opponents by taking their cities; the all-capitals condition belongs to Might. Keep the capital as the initial strategic target, then pursue remaining cities, and finish only on the bridge’s `game_over`. Add a test where capital capture produces another playable state. The inspected bridge reports victory only when the engine reaches `Ended` (`mod/ClaudeBridge/src/Plugin.cs:576`). See [game modes](https://polytopia.fandom.com/wiki/Game_Modes) and [city rules](https://polytopia.fandom.com/zh/wiki/%E5%9F%8E%E5%B8%82).

2. **P0 — The proposed key conflates different commands.** `plan.md:37` omits `accept`, `opponent`, and `effect`, although these are command fields (`docs/PROTOCOL.md:96`). Accepting and declining peace therefore have identical keys—and accept is enumerated first (`LegalActions.cs:39`). Define identity fields **per action kind**, including those fields, and require exactly one match. Reject ambiguous or unsupported kinds. Keep automatic decline, selecting the current action with `accept is False`.

3. **P0 — Coordinate identity can act on the wrong unit.** `recover`, `promote`, `disband`, and `upgrade` are encoded by location rather than unit ID (`mod/ClaudeBridge/src/CommandCodec.cs:66`, `140`). If unit A leaves and B occupies its tile, a queued action can legally affect B. Bind these steps to the original occupant’s ID when resolving the plan, then verify that identity before execution. Similarly, preserve `target_unit_id` for attacks. Keep damage/cost outside identity, but validate material changes separately: a legal attack with different retaliation or a more expensive research action may no longer serve the plan.

4. **P1 — Two consecutive skips is an unsafe dependency rule.** `plan.md:87` allows one failed prerequisite followed by a legal but harmful dependent action: skip the intended attack, then move a supporting unit into exposure. A rejected action has the same problem (`plan.md:90`). Drop the two-skip heuristic. Without explicit dependency metadata, invalidate the remaining plan after the first unresolved step or rejection. Replan within budget; if exhausted, end the turn when legal. Always send the **fresh matched action**, and distinguish “already achieved” from “failed.”

5. **P1 — Define the limits of snapshot-index planning.** `plan.md:40` and `85` correctly recognize research unlocks, but the same limitation applies to moving into attack range, training after vacating a city, and Rider escape destinations. Those future actions may have no index in the original list. Keep “omit `end_turn` = ask again,” and explicitly instruct the model to batch currently available independent actions, ending a batch at an unlock or information boundary. Expose remaining calls in the prompt. Add move→new attack and vacate→train scenarios to `test_turn_runner.py`.

6. **P1 — Enforce three actual CLI invocations, including retries.** Three `planner.plan()` calls can become six subprocess calls because of the internal retry (`plan.md:46`, `73`). Unknown-trigger planning also bypasses the stated cap. Put one invocation budget in the runner and charge every attempt, including invalid responses and trigger calls. Reject booleans as indices (`type(i) is int`), and distinguish an intentionally empty plan from malformed output. Do not silently delete an invalid prerequisite and execute the remainder.

7. **P1 — Make triggers a bounded interrupt with a defined resume rule.** The fixture uses `pending_trigger.type == "CityLevelUp"`, while its actions use `kind == "city_reward"` (`state_city_reward.json:713`, `724`). Dispatch using the real names or legal action kinds. There is no `end_turn` in that fixture, so fallback-to-end cannot handle trigger errors. Specify a legal deterministic fallback for known triggers and a bounded failure outcome for unsupported ones. Resume independent steps after harmless choices, but invalidate plans after consequential changes such as Explorer revelations or Super Unit displacement. Count automatic actions and detect repeated rejected trigger responses.

8. **P1 — Preserve state synchronization explicitly; draining does not establish freshness.** `client.py:172` drains only `_queue`, not unread socket data. It also retains terminal events without raising them. Meanwhile, `Plugin.cs:607` can resend an unchanged state after an accepted command fails to progress. Structure `play_turns()` around one current state: local skips require no read; every sent command requires its result and subsequent state/terminal event; use the returned newest state. Detect unchanged post-action states rather than assuming `ok` means completion. Bound `act()` too: its current `expect("result")` has no timeout (`client.py:135`). Test queued state plus `game_over`, duplicate states, rejection, and accepted-but-unchanged state.

9. **P1 — Establish a clean new-game boundary.** `to_menu_and_new_game()` uses the existing connection (`runner.py:152`), while `wait_until_in_game()` discards only `left_game` (`client.py:159`). Old queued `state` or `game_over` messages can satisfy startup before the new game loads. Flush old-game traffic at a confirmed menu boundary, or reconnect there, then preserve the first new-game state for the loop. Verify its mode/map/opponents before planning. Add a startup test containing old state, old game-over, menu transition, and new state.

10. **P1 — Automatic capture removes useful research ordering.** Capturing before any consultation (`plan.md:81`) increases technology prices before the model can buy tech; the existing prompt itself advises research before expansion (`rules.md:10`). Capture also consumes the capturing unit’s remaining turn. Prefer model-controlled capture ordering, with optional automation for the last enemy city or an explicitly selected policy. At minimum, document the economic tradeoff and invalidate affected steps after an automatic capture. “Every legal capture is strategically mandatory immediately” is too strong.

11. **P1 — Correct the retained rules instead of preserving them wholesale.** `rules.md:11`, `19`, and `20` contain misleading mechanics. Capture normally requires starting the turn on the settlement; Dash grants attack-after-move, not immediate capture. Ordinary attacks consume remaining movement unless an ability such as Escape permits more; “without Dash, attack before moving” implies an unavailable sequence. Support is city level +1, without the additional capital bonus stated here. Melee kill-and-advance should be described as automatic when applicable, followed by inspection of actual positions—not a separately planned move or a guarantee for every unit. Render serialized `abilities`, `can_move`, `can_attack`, and `home` (`observe.py:169`) so the planner can use engine evidence. See [city mechanics](https://polytopia.fandom.com/wiki/City) and [unit skills](https://polytopia.fandom.com/zh/wiki/%E5%8D%95%E4%BD%8D%E6%8A%80%E8%83%BD).

12. **P2 — Make rewards a rush-oriented default, not an unconditional policy.** First correct `rules.md:15`: Resources grants **five stars**, not harvested resources/free population. Resources, Population Growth, and Super Unit are reasonable defaults. Explorer whenever the capital is unknown can repeatedly sacrifice income for redundant scouting; prefer Workshop by default, with an early Explorer when useful fog is nearby. Allow overrides for defensive walls or valuable border expansion. The existing fixture validates only CityWall/Resources, so add every reward pair and chained level-ups. See [city rewards](https://polytopia.fandom.com/wiki/City) and [Explorer tradeoffs](https://polytopia.fandom.com/wiki/Strategies).

13. **P2 — Replace the rigid opening with tactical priorities, and isolate its prompt.** `plan.md:107` mandates Riding→Mining→Smithery; Imperius also needs Climbing on that route, and spending on several technologies can delay the actual rush. Prefer cheap capital growth, Warriors/Riders, nearby productive villages, and Roads or heavier units when the position justifies them. Drop the sub-5-HP attack threshold: coordinated damage, retaliation, escape, and holding the siege through the enemy turn matter more. Treat Chebyshev distance as a lower bound, not a route through terrain and enemy control. Keep these instructions in a separate Domination planner prompt: changing shared `rules.md` also changes the supposedly untouched `ClaudeCodeAgent` through `agent.py:25`. Persist bounded `notes` across turns, separately from the resettable turn log.

14. **P1 — A blocking stdout loop cannot enforce its own timeout on Windows.** `plan.md:56` needs concurrent stdout/stderr readers feeding a queue, with deadline enforcement outside `readline()`, or equivalent asynchronous subprocess handling. Write UTF-8 stdin and **close it**, keep stderr separate, and kill/reap the child on timeout or cancellation; account for descendants retaining pipe handles. This machine resolves `claude` to `C:\Users\augus\.local\bin\claude.exe`, so use that resolved executable with an argument list and `shell=False`. Also sanitize launcher log filenames: copying `agent.name` embeds `claude-code:` (`agent.py:184`, `play_game.py:55`), which is invalid in Windows filenames. See [Python subprocess documentation](https://docs.python.org/3/library/subprocess.html).

15. **P2 — Scope streaming by content block and simplify the extras.** `plan.md:58` merges text and tool JSON into one extractor; track message/block identity, inspect `content_block_start`, and feed only the intended structured-output tool’s `partial_json` into its parser. Reset on retries, handle split Unicode escapes, and validate the final result before executing anything. Commentary-first property order is a hint, not a streaming guarantee; present commentary as model-authored explanation. Test real captured CLI events, including errors and missing final output. See [Anthropic’s streaming event guidance](https://code.claude.com/docs/en/agent-sdk/streaming-output). **I would drop** the two-skip heuristic, blanket auto-capture, rigid technology/HP thresholds, reliance on JSON property order, the unrelated Sandbox documentation change, and the mod-speed stretch work until measurements justify it. Keep the small batch planner, omission convention, automatic peace decline, and simple flushed terminal output.